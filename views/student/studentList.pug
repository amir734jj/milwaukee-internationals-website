extends ../layout

block content
  - if (students.length)
    .row(ng-controller="studentListCtrl")
      a.btn.btn-primary.pull-right(ng-click="getAllStudentsPDF()")
        | Download pdf
      
    table.table.table-hover
      thead
        tr
          th.hidden-xs Date
          th Fullname
          th major
          th.hidden-xs Email
          th Phone
          th Country
          th University
          th.hidden-xs Interest
          th.hidden-xs Action

      each student in students
        tbody
          tr
            td.hidden-xs= student.date
            td= student.fullname
            td= student.major
            td.hidden-xs= student.email
            td= student.phone
            td= student.country
            td= student.university
            td.hidden-xs.interest-list
              each interest in student.interests
                span.label.label-primary.interest-list-item= interest
            td.hidden-xs
              a.btn.btn-danger.delete(href='./delete/' + student.studentId) delete
  - else
    .alert.alert-info
      strong
        | Sorry!
      |  No student has registered yet!
