extends ../layout

block content
  - if (students.length)
    .row(ng-controller="studentListCtrl")
      a.btn.btn-primary.pull-right(ng-click="getAllStudentsPDF()")
        | Download pdf
      
    table.table.table-hover
      thead
        tr
          th Date
          th Fullname
          th major
          th Email
          th Phone
          th Country
          th University
          th Interest
          th Action

      each student in students
        tbody
          tr
            td= student.date
            td= student.fullname
            td= student.major
            td= student.email
            td= student.phone
            td= student.country
            td= student.university
            td.interest-list
              each interest in student.interests
                span.label.label-primary.interest-list-item= interest
            td
              a.btn.btn-danger(href='./delete/' + student.studentId) delete
  - else
    .alert.alert-info
      strong
        | Sorry!
      |  No student has registered yet!
